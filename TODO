* make support for not packerd data (just archived) like *.bz2, *.gz, *.xz, *.lzo
* make support for *.tar.lzo archives (check if thats right name)
* somehow for name linux-headers-3.8.10-1-x86_64.pkg.tar.xz is says that format is "xz"
* test whether simple .tar archives are supported
